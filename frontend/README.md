Frontend scaffold with Vite + React + TypeScript.

Quick start:

```bash
cd TaskFlow/frontend
npm install
npm run dev
```

Next steps:

- Implement task list page that calls backend `GET /tasks`.
- Add board UI with drag-and-drop.
